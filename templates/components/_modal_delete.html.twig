{# Composant qui gère l'ouverture d'une modale de suppression. Il intercepte la soumission du formulaire afin de demander confimation à l'utilisateur #}
{#
    Il prends en paramètre 4 variables :
        - entity_id : correspond à l'id de l'item à supprimer
        - form_id : correspond à l'id du formulaire de suppression
        - title : titre <h3> de la modale
        - content : texte explicatif de la modale

#}

<div id="confirmationModal-{{ entity_id }}" tabindex="-1" aria-hidden="true" class="hidden flex overflow-y-auto overflow-x-hidden fixed top-0 right-0 left-0 z-50 justify-center w-full md:inset-0 h-[calc(100%-1rem)] max-h-full">
    <div class="relative p-4 w-full max-w-2xl max-h-full">
        <div class="relative bg-white rounded-lg shadow dark:bg-gray-700">
            <div class="flex items-center justify-between p-4 md:p-5 border-b rounded-t dark:border-gray-600">
                <h3 class="text-xl font-semibold text-gray-900 dark:text-white">{{ title }}</h3>
                <button type="button" class="text-gray-400 bg-transparent hover:bg-gray-200 hover:text-gray-900 rounded-lg text-sm w-8 h-8 ms-auto inline-flex justify-center items-center dark:hover:bg-gray-600 dark:hover:text-white" id="closeModalButton">
                    <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
                    </svg>
                    <span class="sr-only">Fermer la modal</span>
                </button>
            </div>
            <div class="p-4 md:p-5 space-y-4">
                <p class="text-base leading-relaxed text-gray-500 dark:text-gray-400">{{ content }}</p>
            </div>
            <div class="flex items-center p-4 md:p-5 border-t border-gray-200 rounded-b dark:border-gray-600">
                <button type="button" class="mr-3 py-2.5 px-5 ms-3 text-sm font-medium focus:outline-none bg-white rounded-lg border border-color3-200 hover:bg-color3-100 hover:text-color3-700 focus:z-10 focus:ring-4 focus:ring-color3-100 dark:focus:ring-color3-700 dark:bg-color3-800 dark:text-color3-400 dark:border-color3-600 dark:hover:text-white dark:hover:bg-gray-700" id="cancelButton">Annuler</button>
                <button type="submit" class="text-white bg-color3-700 hover:bg-color3-800 focus:ring-4 focus:outline-none focus:ring-color3-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-color3-600 dark:hover:bg-color3-700 dark:focus:ring-color3-800" id="confirmButton">Accepter</button>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const form = document.getElementById('{{ form_id }}');
        const modal = document.getElementById('confirmationModal-{{ entity_id }}');

        const cancelButton = modal.querySelector('#cancelButton');
        const confirmButton = modal.querySelector('#confirmButton');
        const crossModal = modal.querySelector('#closeModalButton');
        

        form.addEventListener('submit', (event) => {
            event.preventDefault(); 
            modal.classList.remove('hidden');
        });

        cancelButton.addEventListener('click', () => {
            modal.classList.add('hidden');         
            });

        confirmButton.addEventListener('click', () => {
            form.submit();
        });

        crossModal.addEventListener('click', () => {
            modal.classList.add('hidden'); 
        });
    });
</script>