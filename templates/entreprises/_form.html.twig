{{ form_start(form) }}

    {{ include('components/_form_group.html.twig', {
        field: form.nom,
        label: 'Nom',
        placeholder: entreprise.nom
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.adresse,
        label: 'Adresse',
        placeholder: entreprise.adresse
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.tel,
        label: 'Téléphone',
        placeholder: entreprise.tel
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.email,
        label: 'Email',
        placeholder: entreprise.email
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.numero_siret,
        label: 'Siret',
        placeholder: entreprise.getNumeroSiret()
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.rib,
        label: 'RIB',
        placeholder: entreprise.rib
    }) }}


    <button class="my-3 text-white bg-primary-500 border-0 py-2 px-8 focus:outline-none hover:bg-primary-600 rounded text-lg mr-3">Modifier</button>
{{ form_end(form) }}
