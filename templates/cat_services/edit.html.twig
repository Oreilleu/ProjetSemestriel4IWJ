{% extends 'base.html.twig' %}

{% block title %}Edit CatServices{% endblock %}

{% block body %}
    {{ include('components/_title.html.twig', {content: 'Modifier la catégorie' ~ cat_service.nom }) }}

    {{ include('cat_services/_form.html.twig', {'button_label': 'Modifier'}) }}
    
    {{ include('components/_delete_form.html.twig', { 
        item_id: cat_service.id,
        path: 'cat_services',
        labelButton: 'Supprimer',
        classButton: "my-3 text-white bg-primary-500 border-0 py-2 px-8 focus:outline-none hover:bg-primary-600 rounded text-lg mr-3",
        classForm: "inline-block",
        title: "Suppression de la catégorie : " ~ cat_service.nom,
        content: "Êtes-vous sûr de vouloir supprimer cette catégorie ? Tous les produits de cette catégorie n'auront plus de catégorie."  }) 
    }}

    {{ include('components/_link.html.twig', {
        path: 'app_cat_services_index', 
        content: "Liste des catégories"
    }) }}
{% endblock %}
