<form method="post" action="{{ path('app_cat_services_delete', {'id': cat_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat_service.id) }}">
    <button class="text-white bg-primary-500 border-0 py-2 px-8 focus:outline-none hover:bg-primary-600 rounded text-lg">Supprimer</button>
</form>
