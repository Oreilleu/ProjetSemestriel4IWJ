{{ form_start(form, {'attr' : {'class': 'initialDisplay'}}) }}

    {{ include('components/_form_group.html.twig', {
        field: form.superficie,
        label: 'Superficie *'
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.type,
        label: 'Type *'
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.adresse,
        label: 'Adresse *'
    }) }}

    {{ include('components/_form_group.html.twig', {
    field: form.cp,
    label: 'Code postal'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.ville,
    label: 'Ville *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.pays,
    label: 'Pays *'
  }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.id_client,
        label: 'Client *'
    }) }}
        
    {{ include('components/_submit_button.html.twig', {
        content: button_label|default('')
    }) }}
{{ form_end(form) }}