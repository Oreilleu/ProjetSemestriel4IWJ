{% extends 'base.html.twig' %}

{% block title %}Créer un produit{% endblock %}

{% block body %}
    {{ include('components/_title.html.twig', { content: 'Créer un produit' }) }}

    {{ include('produits/_form.html.twig') }}

    {{ include('components/_link.html.twig', {
        path: "app_categories_new",
        content: "Créer une catégorie"
    }) }}
    
    {{ include('components/_link.html.twig', {
        path: "app_produits_index",
        content: "Liste des produits"
    }) }}
{% endblock %}
