{% extends 'base.html.twig' %}

{% block title %}Roles index{% endblock %}

{% block body %}
    <h1>Roles index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for role in roles %}
            <tr>
                <td>{{ role.description }}</td>
                <td>
                    <a href="{{ path('app_roles_show', {'userd_id': role.userd_id}) }}">show</a>
                    <a href="{{ path('app_roles_edit', {'userd_id': role.userd_id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_roles_new') }}">Create new</a>
{% endblock %}
