<form method="post" action="{{ path('app_interractions_delete', {'id': interraction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interraction.id) }}">
    <button class="btn">Delete</button>
</form>
