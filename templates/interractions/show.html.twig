{% extends 'base.html.twig' %}

{% block title %}Interractions{% endblock %}

{% block body %}
    <h1>Interractions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interraction.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_interractions_index') }}">back to list</a>

    <a href="{{ path('app_interractions_edit', {'id': interraction.id}) }}">edit</a>

    {{ include('interractions/_delete_form.html.twig') }}
{% endblock %}
