{% extends 'base.html.twig' %}
{% block title %}S'enregister{% endblock %}
{% block body %}
	<section class="text-gray-600 body-font">
		<div class="container mx-auto flex px-5 py-24 md:flex-row flex-col items-center">
			<!-- Left column -->
			<div class="lg:flex-grow md:w-1/2 lg:pr-24 md:pr-16 flex flex-col md:items-start md:text-left mb-16 md:mb-0 items-center text-center">
				<h1 class="font-title sm:text-4xl text-3xl mb-4 font-medium text-gray-900 dark:text-text-dark-mode-200">
					<br class="hidden lg:inline-block">Découvrez dès maintenant :
				</h1>
				<p class="mb-8 leading-relaxed font-subtitle dark:text-text-dark-mode-200">Des devis sur mesure, Une gestion de facturation dédiée aux PME, Liberté totale pour votre entreprise.</p>
			</div>
			<!-- Right column -->
			<div class="w-full p-8 lg:w-1/2">

				<h2 class="text-2xl font-title text-gray-700 text-center dark:text-text-dark-mode-200">S'inscrire</h2>

				{{ form_start(form) }}
					<div class="mt-4">
						{{ include('components/_form_group.html.twig', {
							field: form.email,
							label: 'Email * :',
						}) }}
					</div>
					<div class="mt-4">
						{{ form_label(form.password.first, 'Mot de passe * :', {'label_attr': {'class': 'block text-sm font-bold mb-2 text-color4-900 dark:text-text-dark-mode-200 bleu:text-black'}}) }}
    					<p>Le mot de passe doit contenir au moins 12 caractères, une lettre majuscule, une lettre minuscule un caractère spécial et un chiffre.</p>	
							<div class="text-red-500 text-sm font-bold mb-3">
        						{{ form_errors(form.password.first) }}
    						</div>
						{{ form_widget(form.password.first, {'attr': {'class': 'text-color4-900 dark:text-text-dark-mode-200 dark:bg-element-dark-mode-600 dark:border-element-dark-mode-400 w-2xl shadow appearance-none border rounded w-full py-2 px-3 leading-tight outline-none focus:ring-2 focus:ring-primary-500 dark:focus:ring-element-dark-mode-400 bleu:text-black bleu:focus:ring-bleu-theme-500 mb-3'}}) }}


						{{ include('components/_form_group.html.twig', {
							field: form.password.second,
							label: 'Répéter le mot de passe * :',
						}) }}

					</div>
					<div class="mt-8">
						{{ include('components/_submit_button.html.twig', {
							content: 'S\'inscrire'
						}) }}
					</div>
				{{ form_end(form) }}
			</div>
		</div>
	</section>
{% endblock %}