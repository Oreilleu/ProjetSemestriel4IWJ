{{ form_start(form) }}
    {{ include('components/_form_group.html.twig', {
        field: form.current_password,
        label: 'Mot de passe actuel',
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.new_password,
        label: 'Nouveau mot de passe',
    }) }}

    {{ include('components/_form_group.html.twig', {
        field: form.confirm_password,
        label: 'Confirmer le nouveau mot de passe',
    }) }}

    <button class="my-3 text-white bg-primary-500 border-0 py-2 px-8 focus:outline-none hover:bg-primary-600 rounded text-lg mr-3">Changer le mot de passe</button>

    {{ include('components/_link.html.twig', {
        path : 'app_account',
        content: 'Mon compte'
    }) }}
{{ form_end(form) }}
