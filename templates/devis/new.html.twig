{% extends 'base.html.twig' %}

{% block title %}New Devis{% endblock %}

{% block body %}
    {{ include('components/_title.html.twig', { content: 'Créer un nouveau devis'}) }}

    {{ include('devis/_form.html.twig') }}

    {{ include('components/_link.html.twig', {
        content: 'Ajouter un lot',
        path: 'app_lots_new'
    }) }}

    {{ include('components/_link.html.twig', {
        content: 'Ajouter un client',
        path: 'app_clients_new'
    }) }}

    {{ include('components/_link.html.twig', {
        content: 'Retour à la liste',
        path: 'app_devis_index'
    }) }}
    
{% endblock %}
