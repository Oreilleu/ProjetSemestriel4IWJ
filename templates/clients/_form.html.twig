
{{ form_start(form) }}
  {{ include('components/_form_group.html.twig', {
    field: form.nom,
    label: 'Nom *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.prenom,
    label: 'Prénom *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.email,
    label: 'Email *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.tel,
    label: 'Téléphone *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.adresse,
    label: 'Adresse *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.cp,
    label: 'Code postal'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.ville,
    label: 'Ville *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.pays,
    label: 'Pays *'
  }) }}

  {{ include('components/_form_group.html.twig', {
    field: form.numero_siret,
    label: 'Siret'
  }) }}

  {{ include('components/_submit_button.html.twig', {
    content: 'Enregistrer le client'
  }) }}
{{ form_end(form) }}