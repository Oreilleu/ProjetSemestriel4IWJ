{% extends 'base.html.twig' %}

{% block title %}ModePaiements{% endblock %}

{% block body %}
    <h1>ModePaiements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_paiement.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mode_paiements_index') }}">back to list</a>

    <a href="{{ path('app_mode_paiements_edit', {'id': mode_paiement.id}) }}">edit</a>

    {{ include('mode_paiements/_delete_form.html.twig') }}
{% endblock %}
