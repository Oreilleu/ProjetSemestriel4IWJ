{% extends 'base.html.twig' %}

{% block title %}ModePaiements index{% endblock %}

{% block body %}
    <h1>ModePaiements index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mode_paiement in mode_paiements %}
            <tr>
                <td>{{ mode_paiement.id }}</td>
                <td>
                    <a href="{{ path('app_mode_paiements_show', {'id': mode_paiement.id}) }}">show</a>
                    <a href="{{ path('app_mode_paiements_edit', {'id': mode_paiement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_mode_paiements_new') }}">Create new</a>
{% endblock %}
