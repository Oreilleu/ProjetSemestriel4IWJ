{% extends 'base.html.twig' %}

{% block title %}Edit Services{% endblock %}

{% block body %}
        <h1 class="font-bold uppercase text-2xl my-5 border-b-black border-b-2">Modifier le produit {{ service.nom }}</h1>

    {{ include('services/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_services_index') }}">Liste des produits</a>

    {{ include('services/_delete_form.html.twig') }}
{% endblock %}
