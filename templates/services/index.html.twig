{% extends 'base.html.twig' %}

{% block title %}Services index{% endblock %}

{% block body %}
    <h1 class="font-bold uppercase text-2xl my-5 border-b-black border-b-2">Liste des produits</h1>

  <ul>
    <li class="p-3 mb-4 border-primary-400 border-2 rounded-xl">
      <article class="grid grid-cols-4 text-center">
        <span>Identifiant</span>
        <span>Catégorie</span>
        <span>Nom</span>
        <span>Prix</span>
      </article>
    </li>
    {% for service in services %}
      <li class="p-5 mb-4 border-primary-400 border-2 rounded-xl">
        <a>
          <article class="grid grid-cols-4 text-center">
              <span>{{ service.id }}</span>
              <span>{{ service.idCatServices ? service.idCatServices.nom : 'Sans catégorie' }}</span>
              <span>{{ service.nom }}</span>
              <span>{{ service.prix }} €</span>
          </article>
        </a>
      </li>
    {% endfor %}

  </ul>

    {# <table class="table">
        <thead>
            <tr>
                <th>Catégorie</th>
                <th>Nom</th>
                <th>Prix</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                <td>{{ c.nom }}</td>
                <td>{{ service.nom }}</td>
                <td>{{ service.prix }} €</td>
                <td>
                    <a href="{{ path('app_services_show', {'id': service.id}) }}">show</a>
                    <a href="{{ path('app_services_edit', {'id': service.id}) }}">edit</a>
                </td>
                <td>
                    {{ include('services/_delete_form.html.twig') }}
                </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    <a href="{{ path('app_services_new') }}">Create new</a>
{% endblock %}
