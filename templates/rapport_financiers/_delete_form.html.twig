<form method="post" action="{{ path('app_rapport_financiers_delete', {'id': rapport_financier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_financier.id) }}">
    <button class="btn">Delete</button>
</form>
